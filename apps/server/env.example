# Local development example (used when running server locally, not in docker-compose).

PORT=8080
HOST=0.0.0.0

JWT_SECRET=dev-secret-change-me

# When using docker compose (infra), Postgres is exposed on localhost:5432 by default.
DATABASE_URL=postgresql://jarvis:jarvis@localhost:5432/jarvis_chat?schema=public

# When using docker compose (infra), Redis is exposed on localhost:6379 by default.
REDIS_URL=redis://localhost:6379

AI_QUEUE_NAME=jarvis_ai_jobs
PUBSUB_CHANNEL=jarvis_room_events

# Optional: used by /translate endpoint
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_TRANSLATE_MODEL=gpt-4o-mini

# Optional: used by /rooms/:roomId/news (Naver News Search API)
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=

# Optional: used by /holidays (Korean Public Data Portal API)
HOLIDAY_API_KEY=

# Production: CORS allowed origins (comma-separated)
# Example: CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGIN=


