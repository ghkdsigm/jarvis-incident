# Local development example (used when running worker locally, not in docker-compose).

# When using docker compose (infra), Postgres is exposed on localhost:5432 by default.
DATABASE_URL=postgresql://jarvis:jarvis@localhost:5432/jarvis_chat?schema=public

# When using docker compose (infra), Redis is exposed on localhost:6379 by default.
REDIS_URL=redis://localhost:6379

AI_PROVIDER=mock
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

AI_QUEUE_NAME=jarvis_ai_jobs
PUBSUB_CHANNEL=jarvis_room_events


