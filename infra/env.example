# Optional: docker compose reads `infra/.env` automatically when you run it from `infra/`.
# If you don't create `infra/.env`, `docker-compose.yml` uses its built-in defaults.

POSTGRES_DB=jarvis_chat
POSTGRES_USER=jarvis
POSTGRES_PASSWORD=jarvis

# Used by apps/server (dev default; change in real deployments)
JWT_SECRET=dev-secret-change-me


